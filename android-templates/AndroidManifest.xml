<?xml version="1.0" encoding="utf-8"?>
<!--
  PLANTILLA del AndroidManifest.xml de ServiMarket, con AdMob + Push ya configurados.

  Cómo usar:
  1) Correr `npx cap add android` (genera android/app/src/main/AndroidManifest.xml)
  2) Reemplazar ese archivo por el contenido de esta plantilla
     (o copiar solo los bloques marcados con <!-- ServiMarket -->).
  3) En producción, cambiar el App ID de AdMob de TEST por el real de tu cuenta AdMob.

  IDs incluidos:
  - AdMob APPLICATION_ID = App ID de TEST de Google (ca-app-pub-3940256099942544~3347511713).
    Reemplazar por el real (formato con `~`) antes de publicar.
  - Package / applicationId = com.servimarket.app (definido en capacitor.config.ts).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- ServiMarket: AdMob App ID (TEST). Reemplazar por el real (ca-app-pub-XXXX~YYYY) en producción. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

    <!-- Permisos -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- ServiMarket: notificaciones push en Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

</manifest>
